# Planning Archive

`docs/archive/planning/` is archive-only.

Rules:
- Do not create new planning surfaces here.
- Treat files here as historical context, not the active source of truth.
- Do not use this public app repo as durable UI/product planning canon.
- If a historical plan still matters, move the work into the current internal planning system instead of reviving the old planning system.
