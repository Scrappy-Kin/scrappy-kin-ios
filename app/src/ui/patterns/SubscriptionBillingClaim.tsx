import AppText from '../primitives/AppText'

export default function SubscriptionBillingClaim() {
  return (
    <AppText intent="supporting">
      Apple manages billing. No Scrappy Kin account, and we do not store your card details or billing address.
    </AppText>
  )
}
