import { APP_REVIEW_PROFILE_EMAIL } from './appReviewTestRecipients'

export const QA_DEVICE_SEND_NOTICE =
  `QA build: use ${APP_REVIEW_PROFILE_EMAIL} as the local profile email to route broker emails to Scrappy Kin test inboxes.`
